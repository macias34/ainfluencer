<%= link_to character_task_result_path(@character, result) do %>
  <div class="min-h-40 flex flex-col gap-2 card card-body" id="<%= dom_id result %>">
    <p class="font-semibold text-lg">
      Result #<%= result.id %>
    </p>
    <p class=" text-content2">
      <%= result.content %>
    </p>
  </div>
<% end %>