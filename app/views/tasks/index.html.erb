<p style="color: green"><%= notice %></p>
<h1>Tasks</h1>
<div id="tasks">
  <% @tasks.each do |task| %>
    <%= render task %>
    <p>
      <%= link_to "Show this task", character_tasks_path(task) %>
    </p>
  <% end %>
</div>
<%= link_to "New task", new_character_task_path %>
