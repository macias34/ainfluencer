<div class="min-h-40 flex flex-col gap-2 card card-body" id="<%= dom_id task %>">
  <p class="font-semibold text-lg">
    <%= task.name %>
  </p>
  <p class=" text-content2">
    <%= task.description %>
  </p>
  <%= button_to "Use", character_task_results_path(@character, task), method: :post, class: "btn btn-primary ", form_class: "w-fit self-end"  %>
</div>